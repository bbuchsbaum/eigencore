% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generalized_schur.R
\name{generalized_schur}
\alias{generalized_schur}
\title{Compute a dense generalized Schur decomposition}
\usage{
generalized_schur(A, B, sort = NULL, vectors = TRUE, ...)
}
\arguments{
\item{A}{Base dense square matrix.}

\item{B}{Base dense square matrix with the same dimension as \code{A}.}

\item{sort}{Optional LAPACK sorting class. Use \code{NULL} or \code{"none"} for no
sorting, \code{"finite"} to move finite generalized eigenvalues first,
\code{"infinite"} to move beta-zero nonzero-alpha eigenvalues first. Custom
predicates and undefined alpha-zero/beta-zero sorting are not part of the
public contract.}

\item{vectors}{Whether to compute Schur vectors \code{Q} and \code{Z}.}

\item{...}{Reserved for future options.}
}
\value{
A classed generalized Schur result with fields \code{S}, \code{T}, \code{Q}, \code{Z},
\code{alpha}, \code{beta}, \code{values}, \code{classification}, \code{sdim}, \code{method}, \code{plan}, and
\code{certificate}.
}
\description{
\code{generalized_schur()} is eigencore's dense QZ surface for general matrix
pencils \verb{A x = lambda B x}. It computes the generalized Schur pair \code{S}, \code{T}
and, when requested, left/right Schur vectors \code{Q}, \code{Z} such that
\verb{A = Q S Z*} and \verb{B = Q T Z*} for complex inputs, with transpose replacing
conjugate-transpose for real inputs. Sparse and operator inputs are not
silently densified.
}
\examples{
A <- matrix(c(0, -1, 1, 0), 2, 2)
B <- diag(2)
qz <- generalized_schur(A, B)
values(qz)

pencil <- generalized_schur(diag(c(2, 3, 0)), diag(c(1, 0, 0)),
                            sort = "infinite")
pencil$classification
}
