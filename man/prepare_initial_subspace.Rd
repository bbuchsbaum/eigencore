% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start.R
\name{prepare_initial_subspace}
\alias{prepare_initial_subspace}
\title{Validate, orthonormalize, and fit a supplied subspace to a start block.}
\usage{
prepare_initial_subspace(
  initial_subspace,
  n,
  width,
  tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{initial_subspace}{User-supplied numeric matrix (or vector) of start
directions.}

\item{n}{Operator domain dimension.}

\item{width}{Number of start-block columns the chosen method consumes
(the Lanczos block size).}

\item{tol}{Numerical-rank tolerance for boundary orthonormalization.}
}
\value{
A list with \code{start} (the \verb{n x width} block) and provenance fields
\code{start_source}, \code{supplied}, \code{accepted}, \code{rejected}, \code{augmented}, \code{rank},
and \code{compressed}. The internal \code{accepted_basis} field retains the full
accepted basis for the invariant-subspace safety guard.
}
\description{
Produces an \verb{n x width} start block for the Lanczos paths together with
provenance counts. Orthonormalization happens here at the solver boundary
for rank detection and honest reporting; the native kernel
re-orthonormalizes and rank-guards the block again before iterating.
}
\details{
When the accepted numerical rank exceeds \code{width} (e.g. a k-column
continuation subspace handed to a scalar method), the start block is a
seeded random rotation of the full accepted basis rather than a truncation:
every supplied direction contributes generic weight to every start column,
so a width-1 start still overlaps all k target directions instead of only
the first. Provenance records this as \code{compressed = TRUE}.

Seed policy: augmented directions are drawn from the active RNG stream
(controlled by the \code{seed} argument of \code{\link[=eig_partial]{eig_partial()}} / \code{\link[=solve]{solve()}}) and
orthonormalized against the accepted directions, matching how the cold
random start is generated.
}
\keyword{internal}
