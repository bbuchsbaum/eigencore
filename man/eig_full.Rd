% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eig_full.R
\name{eig_full}
\alias{eig_full}
\title{Compute a full dense eigendecomposition}
\usage{
eig_full(
  A,
  B = NULL,
  structure = NULL,
  vectors = TRUE,
  tol = 1e-08,
  allow_dense_fallback = c("auto", "never", "always"),
  ...
)
}
\arguments{
\item{A}{Base dense square matrix.}

\item{B}{Optional base dense square matrix for generalized problems.}

\item{structure}{Optional structure descriptor. Use \code{\link[=general]{general()}} to force the
general-pencil path for dense generalized inputs.}

\item{vectors}{Whether to return right eigenvectors.}

\item{tol}{Certification tolerance.}

\item{allow_dense_fallback}{Reserved dense fallback policy. Sparse/operator
inputs still fail unless a future issue explicitly opens an opt-in dense
fallback contract for \code{eig_full()}.}

\item{...}{Reserved for future options.}
}
\value{
An \code{eigencore_eigen_result}.
}
\description{
\code{eig_full()} is the dense/full eigencore surface for standard and
generalized eigenproblems. Sparse and operator inputs are not silently
densified.
}
\examples{
A <- diag(c(1, 4, 9))
B <- diag(c(1, 2, 3))
fit <- eig_full(A, B = B)
values(fit)
certificate(fit)$passed

# Force the dense general-pencil path when alpha/beta diagnostics matter.
pencil <- eig_full(A, B = B, structure = general())
pencil$alpha
pencil$beta
pencil$classification
}
