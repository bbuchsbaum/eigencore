% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/work.R
\name{work}
\alias{work}
\title{Extract typed logical work diagnostics.}
\usage{
work(x, ...)
}
\arguments{
\item{x}{An eigencore result or an existing \code{eigencore_work} record.}

\item{...}{Reserved for future methods.}
}
\value{
An \code{eigencore_work} schema-version-1 record.
}
\description{
Returns shared work counters and phase timings without redefining the
route-specific legacy \code{matvecs} field. Operator, adjoint, metric,
preconditioner, and certification work are reported separately. Unknown
legacy counters are \code{NA} and make \code{complete} false.
}
\details{
\code{schema_version} identifies the frozen record layout and \code{complete}
is true only when every logical counter is known. Each \verb{*_block_calls} field
counts logical applications, while its paired \verb{*_columns} field counts the
vector columns processed by those applications. Certification has distinct
forward-operator and adjoint ledgers. \code{iterations} and \code{restarts} retain
route-level progress, the four \verb{*_seconds} fields report measured phase or
total elapsed time when available, and \code{legacy_matvecs} preserves the result's
historical route-specific \code{matvecs} value without treating it as a common
unit across solver families.
}
\examples{
fit <- eig_partial(diag(c(5, 3, 1)), k = 2)
work(fit)$operator_columns
work(fit)$legacy_matvecs
}
