% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{solve.eigencore_plan}
\alias{solve.eigencore_plan}
\title{Execute a frozen eigencore solver plan.}
\usage{
\method{solve}{eigencore_plan}(
  a,
  b,
  restart_state = NULL,
  reuse = c("auto", "basis_only", "same_operator"),
  retain_state = c("none", "basis", "same_operator"),
  replan = FALSE,
  ...
)
}
\arguments{
\item{a}{An executable \code{eigencore_plan}.}

\item{b}{Unused second argument reserved by the base \code{\link[=solve]{solve()}} generic.}

\item{restart_state}{Optional reusable state. Implemented by the reusable-
restart-state layer.}

\item{reuse}{Reuse policy for an optional restart state.}

\item{retain_state}{State-retention request for the returned result.}

\item{replan}{Whether to create and execute a new plan from the embedded
problem under current policy. The original plan is not modified.}

\item{...}{Must be empty; execution controls are frozen in the plan.}
}
\value{
An \code{eigencore_eigen_result} or \code{eigencore_svd_result} containing the
exact plan that governed execution.
}
\description{
\code{solve(plan)} validates and runs the problem, method, controls, policy, and
execution arguments captured by \code{\link[=plan_solver]{plan_solver()}}. It never invokes the
planner unless \code{replan = TRUE}. Execution arguments cannot be overridden
through \code{...}.
}
