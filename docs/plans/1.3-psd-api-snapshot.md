# Eigencore 1.3 PSD API snapshot

Status: normative companion to `1.3-psd-contract.md`.

This file is the human-readable source for the implementation snapshots. The
test suite must compare installed exports, signatures, registrations, class
field names, certificate names, condition fields/codes, and capability keys to
these exact entries. Refreshing a snapshot requires a contract amendment and a
NEWS entry.

## Additive exports and exact signatures

```text
psd_tolerance(abs = 0, rel = sqrt(.Machine$double.eps))
psd_policy(symmetry = psd_tolerance(), positivity = psd_tolerance(rel = 64 * .Machine$double.eps), rank = psd_tolerance(), rhs = psd_tolerance(), scale = "frobenius", symmetry_repair = c("average", "reject"), negative_repair = c("clip", "reject"), structure_repair = c("canonicalize", "reject"))
psd_identity(dim, policy = psd_policy())
psd_factor(x, policy = psd_policy(), source = c("snapshot", "live"))
psd_gram_factor(x, orientation = c("columns", "rows"), policy = psd_policy(), source = c("snapshot", "live"))
psd_laplacian(x, policy = psd_policy(), source = c("snapshot", "live"))
psd_capabilities(x)
psd_spectrum(x, repaired = FALSE)
psd_rank(x)
psd_nullity(x, type = c("numerical", "algebraic"))
psd_apply(x, X, action = c("form", "sqrt", "inverse_sqrt", "pseudoinverse", "image_projector", "null_projector"))
psd_operator(x, action = c("form", "sqrt", "inverse_sqrt", "pseudoinverse", "image_projector", "null_projector"))
psd_reduce(x, X)
psd_lift(x, Z)
psd_solve(x, B, tolerance = NULL)
psd_gram(x, X, Y = NULL)
psd_orthonormalize(x, X, required_rank = NULL, tolerance = NULL)
psd_reduced_operator(x, A)
```

No alias, short name, or compatibility synonym is admitted in 1.3.

## Additive S3 registrations

```text
as_operator.eigencore_psd_factor
print.eigencore_psd_block_result
print.eigencore_psd_capabilities
print.eigencore_psd_certificate
print.eigencore_psd_factor
print.eigencore_psd_policy
print.eigencore_psd_solve_result
```

## Class vectors

```text
eigencore_psd_tolerance
eigencore_psd_policy
eigencore_psd_factor
eigencore_psd_capabilities
eigencore_psd_solve_result
eigencore_psd_block_result
eigencore_psd_serialization
c("eigencore_psd_certificate", "eigencore_certificate")
c("eigencore_psd_invalid_input", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_asymmetry_error", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_indefinite_error", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_unsupported_action", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_incomplete_evidence", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_stale_source", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_incompatible_rhs", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_nonportable", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_corrupt_state", "eigencore_psd_error", "error", "condition")
c("eigencore_psd_infeasible_block", "eigencore_psd_error", "error", "condition")
```

## Record field names

### `eigencore_psd_tolerance`

```text
schema_version abs rel
```

### `eigencore_psd_policy`

```text
schema_version scale symmetry positivity rank rhs symmetry_repair negative_repair structure_repair
```

### `eigencore_psd_factor`

```text
schema_version dim dtype representation method policy scale thresholds spectrum classification rank nullity algebraic_nullity rank_bounds evidence capabilities operator_identity source_identity source_semantics materialization certificate work memory serialization warnings
```

### `spectrum`

```text
coverage original repaired category source_index retained_indices exact_zero_indices accepted_negative_indices numerical_null_indices signed_zero_count lower_bound upper_bound
```

### `classification`

```text
retained_positive exact_zero accepted_negative numerical_null materially_negative user_truncated
```

### `thresholds`

```text
scale_definition symmetry positivity rank
```

### `rank_bounds`

```text
lower upper
```

### `evidence`

```text
schema_version spectrum_coverage validation action_fidelity source_semantics theorem bound_type details
```

### `eigencore_psd_capabilities`

```text
schema_version representation evidence form sqrt inverse_sqrt pseudoinverse image_projector null_projector reduction lift strict_solve gram orthonormalize reduced_operator numerical_rank numerical_nullity algebraic_nullity serialization cache_reuse
```

Each capability entry:

```text
available fidelity evidence_required materialization reason
```

### `materialization`

```text
source factor dense_n_by_n notes
```

### `eigencore_psd_solve_result`

```text
schema_version solution compatibility_defect compatibility_threshold compatible factor_identity certificate work warnings
```

### `eigencore_psd_block_result`

```text
schema_version basis rank required_rank condition dropped gram postcondition_error factor_identity certificate work warnings
```

### `eigencore_psd_serialization`

```text
schema_version portable originating_session incompatibility_reason integrity_token source_identity_token operator_identity_token policy_token state_token reconstruction
```

### `eigencore_psd_certificate`

The common certificate prefix is retained first, followed by PSD fields:

```text
passed tolerance orthogonality_tolerance orthogonality_required certificate_type norm_bound_type scale_is_estimate max_backward_error max_residual max_orthogonality_loss orthogonality_passed failed_indices scale notes residuals backward_error orthogonality converged schema_version scope thresholds source_identity factor_identity representation evidence repair_applied symmetry_defect repair_defect source_action_defect original_spectrum repaired_spectrum classification rank nullity algebraic_nullity rank_bounds capabilities action_bounds
```

## Condition fields and codes

Every PSD condition begins with:

```text
message call code field expected actual source_identity factor_identity representation capability evidence scale threshold defect indices details
```

Frozen codes:

```text
invalid_dimension invalid_dtype nonfinite_input invalid_policy invalid_structure asymmetric_input indefinite_input unsupported_action incomplete_evidence stale_source incompatible_rhs session_incompatible unsupported_schema corrupt_factor infeasible_block_rank
```

## Enum values

```text
representation: identity diagonal dense_spectral gram_dense gram_sparse laplacian_sparse
method: "analytic identity PSD factor" "analytic diagonal PSD factor" "complete dense symmetric eigen PSD factor" "complete dense Gram compact-SVD PSD factor" "structural sparse Gram PSD factor" "structural sparse graph-Laplacian PSD factor"
spectrum_coverage: complete structural partial estimated
validation: computed supplied_certified user_declared unavailable
action_fidelity: exact_for_certified_factor repaired_with_defect approximate_with_bound
source_semantics: immutable_snapshot live_versioned_source uncacheable_opaque_source
spectrum_category: retained_positive exact_zero accepted_negative numerical_null materially_negative user_truncated
action: form sqrt inverse_sqrt pseudoinverse image_projector null_projector
certificate_type: psd_factor_validation psd_strict_solve psd_block_orthonormalization
repair: average reject clip canonicalize
materialization source: identity diagonal_vector dense_matrix sparse_csc live_operator
materialization factor: analytic_identity analytic_diagonal dense_eigen compact_svd sparse_gram sparse_laplacian
psd_factor Matrix inputs: ddiMatrix dgeMatrix dsyMatrix dpoMatrix
psd_gram_factor Matrix inputs: dgeMatrix dgCMatrix
psd_laplacian Matrix inputs: dgCMatrix dsCMatrix
```

## Existing surfaces that remain unchanged

```text
eigen_problem(metric = ) remains SPD/Hermitian-metric-only
eig_partial(A, B = ) remains SPD-only for Hermitian problems
eigencore_operator_identity remains schema version 1
eigencore_work remains schema version 1 with no new fields
eigencore_memory remains schema version 1 with no new fields
eigencore_plan and eigencore_restart_state remain schema version 1
existing result and residual-certificate field snapshots retain their order
```
