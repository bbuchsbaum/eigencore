# V2 CRAN Benchmark Manifest

Date: 2026-07-03

This manifest is the runnable benchmark inventory for the V2 CRAN release
candidate. It is not a release signoff by itself. A row is release-grade only
when it was run from a clean installed package, saved its result artifacts, and
passed the strict gate for the promoted solver family.
`V2 CRAN` is the internal release-boundary name used by the tracker and audit
files; the package version for this release candidate is `eigencore` 1.0.0.

The companion documentation scope audit is
`docs/v1-doc-scope-audit.md`. That audit maps user-facing docs, migration
docs, design notes, and release evidence to V1 requirements; it does not relax
the benchmark stop rule below.

The companion completion audit is `docs/v1-completion-audit.md`. It is the
single checklist to consult before any V1 completion claim.

The contribution-facing interpretation is
`docs/contribution-methods-artifact.md`. It summarizes the current benchmark
wins, losses, and non-promoted solver boundaries without changing the runnable
commands below.

V3 promotion gates live separately in
`docs/post-v1-benchmark-gates.md` and
`inst/benchmarks/post-v1-gate-manifest.R`. Those gates are for broadening
solver claims after the V2 CRAN release; they do not change the CRAN release evidence
below.

The source-tree all-surface quick smoke is:

```sh
Rscript inst/benchmarks/run-post-v1-gates.R --tier=smoke --gates=post_v1_svd_hard_surface,post_v1_operator_sidecars,post_v1_randomized_svd_hard_surface,post_v1_generalized_preconditioned_surface,post_v1_shift_invert_boundaries,post_v1_nonsymmetric_matrix_free_surface --load-all
```

It is a contract/provenance regression check. Speed and memory ratios printed
by quick fixtures are diagnostic unless the installed-package strict gate below
also supports the promoted release claim.

Use an installed package path for release evidence, for example:

```sh
R CMD INSTALL --library=/tmp/eigencore-bench-lib .
R_LIBS=/tmp/eigencore-bench-lib Rscript inst/benchmarks/<script>.R ...
```

## Release Surfaces

| Surface | Script | Primary command | Saved artifacts | Current status |
|---|---|---|---|---|
| G1 Hermitian native gate | `inst/benchmarks/bench-native-hermitian-gate.R` | `R_LIBS=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-native-hermitian-gate.R --strict --save --cases=path_laplacian:1000` | `*-native-hermitian-gate-rows.rds`, `*-native-hermitian-gate-summary.rds` | Green for the promoted structured-tridiagonal default. Installed strict evidence from 2026-06-05 saved `20260605-native-hermitian-gate-rows.rds` and `20260605-native-hermitian-gate-summary.rds`; `path_laplacian:1000` certified `20/20`, passed speed (`6.15x` versus RSpectra), memory (`1.07x` versus best reference), and PRIMME parity (`22.58x`). |
| G1 sparse Hermitian coverage | `inst/benchmarks/bench-hermitian-sparse.R` | `R_LIBS=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-hermitian-sparse.R --strict --save --cases=<case:id>` | `*-hermitian-sparse-rows.rds`, `*-hermitian-sparse-summary.rds` | Diagnostic. Used with case filtering for sparse Hermitian reruns; current sparse block auto-promotion is disabled except diagnostic opt-in. |
| G2 2D grid Laplacian diagnostic | `inst/benchmarks/bench-grid-laplacian.R` | `R_LIBS=/tmp/eigencore-bench-lib-v2 Rscript inst/benchmarks/bench-grid-laplacian.R --strict --save --iterations=1 --methods=eigencore,RSpectra`; source smoke: `Rscript -e 'pkgload::load_all("."); source("inst/benchmarks/bench-grid-laplacian.R")' --args --quick --strict --iterations=1 --methods=eigencore,RSpectra` | `20260607-grid-laplacian-rows.rds`, `20260607-grid-laplacian-gates.rds` | Green diagnostic prototype for explicitly tagged separable 2D path-grid Laplacians only. Installed 2026-06-07 evidence on `grid_2d_path:100x100`, `k = 10` certifies `10/10` in original coordinates, does not materialize a dense operator, and reports about `101.65x` speed versus certified RSpectra with `0.91x` memory ratio. Plain sparse matrices with the same grid pattern are not auto-recognized and continue through the existing sparse Hermitian Lanczos boundary. |
| H production SVD | `inst/benchmarks/bench-svd-surface.R` | Strict installed production H gate: `R_LIBS_USER=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-svd-surface.R --iterations=3 --h-candidate --methods=eigencore,RSpectra,irlba --cases=tall_sparse,wide_sparse --subject=eigencore --strict --save`; smallest/interior target contract probe: `Rscript -e 'pkgload::load_all("."); source("inst/benchmarks/bench-svd-surface.R")' --args --iterations=1 --strict --cases=smallest_sparse:5000x500,interior_sparse:5000x500 --methods=eigencore_smallest,eigencore_interior,base_smallest,base_interior --subject=eigencore_smallest`; quick `tall_sparse:600x90` / `wide_sparse:90x600` probes remain diagnostic and are not release signoff. Retained diagnostics may still use `--subject=eigencore_irlba_lbd_retained_bpro`; normal-scout diagnostics may use `--methods=eigencore_irlba_lbd_normal_scout,eigencore_golub_kahan_one_sided,RSpectra --subject=eigencore_irlba_lbd_normal_scout`. | `20260715-svd-surface-rows.rds`, `20260715-svd-surface-gates.rds`, `*-svd-surface-target-contracts.rds`, `20260715-svd-surface-memory.rds`, `20260715-svd-projected-stop-comparison.rds` | Green for the promoted non-quick tall/wide sparse H surface and the sparse target contract rows. Fresh installed 2026-07-15 evidence, with matched `tol` passed to RSpectra and irlba, certifies `20/20` triplets for every method on `tall_sparse` (`100000 x 500`) and `wide_sparse` (`500 x 100000`). Eigencore uses the bounded native Gram special case with `native_gram_eigensolver = "lapack_dsyevr"`, `materialized_gram = TRUE`, and no fallback; it passes certified-total speed/memory at `2.16x` / `2.05x` tall and `7.18x` / `2.15x` wide versus RSpectra, the best certified reference. Fresh 2026-06-06 source strict target-contract evidence certifies `smallest_sparse:5000x500` through `native certified Gram SVD special case` at about `185x` speed and `14x` memory versus `base_smallest`, and `interior_sparse:5000x500` through `native full-subspace interior Golub-Kahan SVD` at about `2.2x` speed and `2.1x` memory versus `base_interior`. The `complex_dense` case is benchmark-visible for native dense complex SVD and excluded from the sparse H release gate. Retained IRLBA/LBD/BPRO diagnostics remain benchmark-visible but unpromoted. |
| H2 Gram-SVD cutoff and crossover evidence | `inst/benchmarks/bench-svd-gram-cutoff.R` | Cutoff: `R_LIBS=/tmp/eigencore-bench-lib-v2 Rscript inst/benchmarks/bench-svd-gram-cutoff.R --strict --save --iterations=3 --gram-max=1024 --methods=eigencore,RSpectra,irlba`; controlled crossover: add `--crossover --iterations=5`; quick structural crossover smoke: add `--quick --crossover --iterations=1` | `*-svd-gram-cutoff-{rows,gates,memory,contracts,policy}.rds`; controlled evidence: `20260715-svd-crossover-{rows,gates,memory,contracts,policy,summary}.rds` | The cutoff rows remain the no-broad-raise boundary. In crossover mode, strict execution additionally requires every external row to certify, complete finite summaries, and a sampled certified-total transition from slower to faster in both orientations; raw-call crossover remains a separate diagnostic. The installed 2026-07-15 controlled sweep holds the small side at 90, rank at 5, density at 0.003, and uses nested sparse fixtures while varying only the long side over 600/5,000/25,000/100,000 in both orientations. All eigencore rows use materialized `lapack_dsyevr`, certify in original coordinates, and avoid Golub-Kahan fallback; all RSpectra and irlba rows also pass the matched certificate. Eigencore is slower at 600. Certified-total ratios versus RSpectra cross above 1 at 5,000 (`1.19x` tall, `3.88x` wide); raw solver ratios cross at 25,000 tall (`1.62x`) and 5,000 wide (`2.71x`). At 100,000 the raw/certified ratios are `1.61x` / `2.97x` tall and `18.91x` / `21.51x` wide. |
| I randomized SVD | `inst/benchmarks/bench-randomized-rsvd.R` | Strict scoped release plus controller contract: `Rscript -e 'pkgload::load_all("."); source("inst/benchmarks/bench-randomized-rsvd.R")' --args --iterations=1 --strict --cases=exact_low_rank_dense:2000x500,low_rank_sparse:2000x500 --methods=eigencore_randomized,rsvd`; quick diagnostics: `--quick --iterations=5 --methods=eigencore_randomized,rsvd` | `20260605-randomized-rsvd-rows.rds`, `20260605-randomized-rsvd-gates.rds`, `*-randomized-rsvd-controller-contracts.rds`; fresh local installed rerun used `/tmp/eigencore-rand-native-lib` | Green for the scoped dense release row and sparse native-controller contract. Fresh source 2026-06-06 strict evidence certifies `exact_low_rank_dense:2000x500` for eigencore and `rsvd`, reports `randomized_controller_native = TRUE`, `randomized_dense_native_controller = TRUE`, `randomized_native_certificate_diagnostics = TRUE`, `randomized_adaptive_stop_used = TRUE`, `randomized_sketch_kind = "native_fused_a_omega"`, `randomized_projection_kind = "native_direct_qt_a"`, and `randomized_core_solver = "native_dense_projected_svd"`, and passes speed/accuracy at about `2.55x` versus certified `rsvd`. The same strict run certifies `low_rank_sparse:2000x500` through `native_csc_randomized_controller` with `randomized_sparse_native_controller = TRUE` and the controller contract green; its `1.24x` speed ratio remains diagnostic, not a promoted sparse release claim. Nearly-low-rank and slow-decay rows remain printed diagnostics with `release_gate_required = FALSE` because `rsvd` fails eigencore certification. LU/none and matrix-free randomized control remain reference-labelled. |
| J generalized SPD LOBPCG | `inst/benchmarks/bench-generalized-lobpcg.R` | Full installed scoped gate: `R_LIBS_USER=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-generalized-lobpcg.R --iterations=1 --strict --save`; focused sparse slices may use `--cases=sparse_generalized_path_smallest:1000` or `--cases=sparse_generalized_path_largest:1000` with `--methods=eigencore_shifted_tridiagonal,base --subject=eigencore_shifted_tridiagonal --strict`; dense auto fallback boundary remains inspectable with `--cases=dense_generalized_partial_smallest:180,dense_generalized_partial_largest:180 --methods=eigencore_auto,base --strict` | `20260605-generalized-lobpcg-rows.rds`, `20260605-generalized-lobpcg-gates.rds`, `20260605-generalized-lobpcg-native-contracts.rds`, `20260605-generalized-lanczos-reference-contracts.rds`, `20260605-generalized-lobpcg-adversarial-b-contracts.rds` | Green for the promoted sparse shifted-tridiagonal generalized LOBPCG surface. Fresh installed saved evidence on 2026-06-05 certifies `10/10` for both sparse-smallest and sparse-largest at `n = 1000`, keeps native generalized/B-orthogonal/shifted-tridiagonal provenance visible, and passes speed/memory versus dense base at about `1.56x` / `8.8x` for smallest and `1.67x` / `25.0x` for largest. Dense generalized `auto()` stays on the native dense LAPACK fallback, and generalized Lanczos plus adversarial B rows remain contract/reference diagnostics rather than broader production claims. |
| J2 generalized eigen replacement | `inst/benchmarks/bench-generalized-eigen.R` | Quick strict smoke: `Rscript inst/benchmarks/bench-generalized-eigen.R --quick --iterations=1 --strict`; full installed gate: `R_LIBS=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-generalized-eigen.R --iterations=3 --strict --save` | `*-generalized-eigen-rows.rds`, `*-generalized-eigen-gates.rds` | Certification/planner gate for dense full generalized pencils (`eig_full`), sparse diagonal-`B` general-pencil partial Arnoldi, explicit unsupported sparse boundaries, and dense QZ/GSVD helpers. Speed ratios are diagnostic; strict gate requires certificate/oracle match, no sparse densification on the promoted partial path, and honest unsupported labels on out-of-scope sparse pencils. |
| K generalized SPD B-orthogonal Lanczos | `inst/benchmarks/bench-generalized-lobpcg.R` focused K rows plus focused tests | Focused probe: `Rscript -e 'pkgload::load_all("."); source("inst/benchmarks/bench-generalized-lobpcg.R")' --args --quick --strict --iterations=1 --cases=diagonal_generalized_block_lanczos_native_smallest,diagonal_generalized_lanczos_native_smallest,dense_generalized_lanczos_native_largest,sparse_tridiagonal_b_generalized_lanczos_native_metric_smallest,sparse_csc_generalized_lanczos_ref_cholesky_smallest --methods=eigencore_lanczos_block_native,eigencore_lanczos_native,eigencore_lanczos_reference,eigencore,base`; source test: `Rscript -e 'pkgload::load_all("."); testthat::test_file("tests/testthat/test-generalized-lobpcg.R", reporter="summary")'` | `*-generalized-lanczos-block-native-contracts.rds`, `*-generalized-lanczos-native-contracts.rds`, `*-generalized-lanczos-reference-contracts.rds`, plus the K rows in `*-generalized-lobpcg-rows.rds` | Green for native dense/diagonal transformed generalized Lanczos, including the explicit block transformed row, plus sparse CSC metric-solve boundary diagnostics. The source 2026-06-06 focused strict probe certifies `diagonal_generalized_block_lanczos_native_smallest` with `block_size = 2` and the block-native contract green. The sparse tridiagonal CSC row records `native_sparse_tridiagonal_thomas` / `tridiagonal_thomas` metric-solve provenance inside the reference-labelled Lanczos loop; the general sparse CSC row remains `Matrix::Cholesky` reference provenance. Sparse-CSC block production Lanczos and arbitrary sparse-CSC native factorization are not claimed. |
| M standard preconditioned LOBPCG | `inst/benchmarks/bench-lobpcg-preconditioned.R` | Diagnostic/no-promotion probe: `Rscript -e 'pkgload::load_all(".", recompile = FALSE); source("inst/benchmarks/bench-lobpcg-preconditioned.R")'`; strict form is expected to fail until a redesign beats the current eigencore default. | `*-lobpcg-preconditioned-rows.rds`, `*-lobpcg-preconditioned-gates.rds` | Closed no-promotion decision. Native shifted-tridiagonal rows certify and beat certified external references on `n = 200, 1000, 2000`, `k = 5`, but fail the scalar-speed gate versus the current eigencore scalar/tridiagonal default, with speed ratios about `0.65`, `0.32`, and `0.14` in the 2026-06-06 source probe. Keep standard Hermitian LOBPCG diagnostic/prototype-labelled. |
| L shift-invert contract | `inst/benchmarks/bench-shift-invert.R` | Full installed scoped gate: `R_LIBS_USER=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-shift-invert.R --iterations=1 --strict --save`; quick full label surface: `--quick --strict --iterations=1 --save`; user-solve boundary: `--quick --strict --iterations=1 --cases=matrix_free_user_solve_reference` | `20260605-shift-invert-rows.rds`, `20260605-shift-invert-contracts.rds` | Green for the scoped V1 shift-invert surface. Fresh installed 2026-06-05 saved evidence passes all eight native/reference contract rows. Dense standard, dense generalized, diagonal standard, sparse symmetric-tridiagonal standard, and sparse/diagonal tridiagonal-generalized rows certify `6/6` in original coordinates with native factorization-cache provenance; general sparse standard and sparse/diagonal generalized rows remain nonnative `Matrix::lu` reference boundaries with estimated-scale certificate honesty; the matrix-free user-solve row remains nonnative with `user_solve` external-cache provenance and exact original-coordinate certification. Current strict gates also require `shift_invert_factorization_contract_v1` metadata so native labels are allowed only for eigencore-owned factorized apply. |
| Nonsymmetric compatibility | `inst/benchmarks/bench-nonsymmetric.R` | `R_LIBS_USER=/tmp/eigencore-bench-lib Rscript inst/benchmarks/bench-nonsymmetric.R --iterations=1 --save --strict`; quick strict smoke: `--quick --strict --iterations=1`; focused matrix-free installed row: `--strict --iterations=1 --cases=matrix_free_nonnormal` | `20260605-nonsymmetric-rows.rds`, `20260605-nonsymmetric-contracts.rds` | Green for the scoped V1 compatibility surface, with a current V2 tranche for dense/sparse CSC refined extraction. Fresh installed evidence on 2026-06-05 certifies the V1 dense and sparse CSC native Arnoldi rows with `native_arnoldi_label = TRUE`, `ritz_extraction_native = TRUE`, and exact right-residual contracts. The current source script now distinguishes dense/sparse `native_refined_arnoldi_label = TRUE`, `arnoldi_extraction = "refined_ritz"`, and `refined_extraction_native = TRUE`; it also carries a dense complex LAPACK row with `native_dense_complex_label = TRUE`. A focused installed strict run on 2026-06-06 certifies `matrix_free_nonnormal:30` with `native_matrix_free_arnoldi_label = TRUE`, `matrix_free_native = TRUE`, restart diagnostics, and exact right-residual certification. Dense real rows use full dense subspace compatibility; sparse CSC and matrix-free rows keep bounded subspace/restart policy. Adjoint-capable rows also return left vectors with left-residual and biorthogonality diagnostics; full Krylov-Schur or harmonic/interior extraction, matrix-free refined extraction, and native complex sparse/operator paths remain future scope. |

## Diagnostic And Baseline Surfaces

| Surface | Script | Purpose |
|---|---|---|
| Post-V1 operator sidecars | `inst/benchmarks/bench-post-v1-operator-sidecars.R` | Smoke/strict provenance gate for callback boundaries, including native matrix-free SVD, native matrix-free nonsymmetric Arnoldi, and native generalized-B callback contracts. |
| Block Hermitian prototype | `inst/benchmarks/bench-block-hermitian-prototype.R` | Development diagnostics for block Hermitian variants before promotion. |
| G1 candidate baseline | `inst/benchmarks/bench-g1-candidate-baseline.R` | Rebuilds `inst/benchmarks/baselines/g1_candidate_pre.csv` for before/after G1 candidate comparisons. |
| Performance baseline | `inst/benchmarks/bench-performance-baseline.R` | Broad comparative smoke across eigen, SVD, and generalized cases; not a strict release gate. |
| SVD tall-skinny | `inst/benchmarks/bench-svd-tallskinny.R` | Focused SVD development surface retained for historical tall-skinny diagnostics. |
| Tiny Gram eigensolvers | `inst/benchmarks/bench-tiny-gram-eigensolvers.R` | Tracks tiny dense Gram solve choices used by bounded SVD special cases. |
| Reference suite | `benchmarks/reference_suite.R` | Legacy reference comparisons; use the explicit `inst/benchmarks` release surfaces for V1 gate evidence. |

## Stop Rule

Do not use this manifest to close V2 CRAN by itself. Benchmark signoff still
requires:

1. Every promoted solver family to pass its strict installed-package gate.
2. Red rows above to be fixed, demoted, or explicitly scoped out by a PRD
   revision.
3. Saved artifacts from the final runs to be listed in `benchmarks/RELEASES.md`
   with the command that produced them.
4. A fresh `R CMD build` and `R CMD check --no-manual` after the final
   benchmark/doc updates.
