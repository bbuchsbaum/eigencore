# Target the largest imaginary part.

Target the largest imaginary part.

## Usage

``` r
largest_imaginary()
```

## Value

An `eigencore_target` descriptor selecting values by largest `Im(x)`
(ARPACK `LI`).
