# Target the smallest imaginary part.

Target the smallest imaginary part.

## Usage

``` r
smallest_imaginary()
```

## Value

An `eigencore_target` descriptor selecting values by smallest `Im(x)`
(ARPACK `SI`).
