# Extract right singular vectors.

Extract right singular vectors.

## Usage

``` r
right_vectors(x, ...)
```

## Arguments

- x:

  An eigencore SVD or nonsymmetric eigen result object.

- ...:

  Reserved for future methods.

## Value

A matrix of right singular vectors or right eigenvectors, or `NULL` when
the result does not contain a right-vector field.
