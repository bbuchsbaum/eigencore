# Guard: error when `initial_subspace` reaches an unsupported plan.

Guard: error when `initial_subspace` reaches an unsupported plan.

## Usage

``` r
validate_initial_subspace_plan_support(problem, plan)
```

## Value

Invisibly `TRUE` when the plan is supported; otherwise throws an error.
